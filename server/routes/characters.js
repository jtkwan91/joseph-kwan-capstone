// const express = require("express")
// const router = express.Router()
// const fs = require("fs")

// // to do: get the buffer from express 
// const raw = await axios.get(url.toString(), { responseType: "arraybuffer" })
// const buffer = Buffer.from(raw.data, "binary")

// const data = await imagemin.buffer(buffer, {
//   plugins: [
//     imageminWebp({
//       method: 6,
//       quality: 60,
//       autoFilter: true,
//       metadata: "none",
//       resize: size.xlarge
//     })
//   ]
// })
// return db.set(newk, data.toString("base64"))

// module.exports = router